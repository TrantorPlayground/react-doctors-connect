import React from 'react';
import {Layout, List} from "antd";
import Header from "../../layout/header";

const Home: React.FC = () => {
    return (
        <Layout>
            <Header/>
        </Layout>
    );
};

export default Home;